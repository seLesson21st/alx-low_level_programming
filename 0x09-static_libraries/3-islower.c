#include "main.h"

/**
 * _islower - fanction that checks for lowercase
 * @c: Character to be checked
 * Return: 1 if chacters is lowercase, o if not
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
