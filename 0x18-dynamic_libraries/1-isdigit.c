#include "main.h"

/**
 * _isdigit - fanction that checks for a digit (0 to 9)
 * @c: integer to be cchecked
 * Return: 1 if c is a digit, 0 if not
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
