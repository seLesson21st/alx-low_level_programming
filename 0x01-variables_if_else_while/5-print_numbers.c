#include <stdio.h>

/**
 * main - Print single digit numbers.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int b;

	for (b = 0; b < 10; b++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
